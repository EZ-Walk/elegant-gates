{
  "buildCommand": "cd services/map && npm install && npm run build",
  "outputDirectory": "services/map/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
